# Example server environment variables
# Optional: set to 'true' to enable calling an external LLM (OpenAI) for chat replies
USE_EXTERNAL_LLM=false
# Required when USE_EXTERNAL_LLM=true
OPENAI_API_KEY=
# Optionally override the server port
PORT=3002
